services:
  - type: web
    name: card-news-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    envVars:
      - key: API_SECRET
        sync: false
